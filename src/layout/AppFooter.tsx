/* eslint-disable @next/next/no-img-element */
const AppFooter = () => {
    return (
        <div className="layout-footer">
            CIM ( Customer Interaction Managment)
        </div>
    );
};

export default AppFooter;
